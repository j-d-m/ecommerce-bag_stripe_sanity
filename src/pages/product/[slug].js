// *In this page we are creating a new slug for each product. We have named the file [slug].js this gives us a dynamic file name - remember a slug is a unique identifier for each product*/

import React from "react";

const ProductDetails = () => {
  return <div>PRODUCT</div>;
};

export default ProductDetails;
